import LandingPage from "@/components/landing/LandingPage";

export const metadata = {
  title: "LocalLayer — Your neighborhood, connected",
  description:
    "Real-time local updates, alerts, and community posts on a live map. Share emergencies, events, and news within your radius.",
};

export default function Home() {
  return <LandingPage />;
}
